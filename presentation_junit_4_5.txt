		

1: supprimer la dependance avec juint 4 :
	ajouter l'exclusion dans  <artifactId>spring-boot-starter-test</artifactId>
		
		<exclusions>
                <exclusion>
                    <groupId>junit</groupId>
                    <artifactId>junit</artifactId>
                </exclusion>
            </exclusions>

ps: les annotations ne sont plus reconnu

2 - pour rendre les tests existant executable avec junit 5 sans toucher au code rajouter seulement la dépendance:
			<dependency>
                <groupId>org.junit.vintage</groupId>
                <artifactId>junit-vintage-engine</artifactId>
            <version>5.3.2</version>
            <scope>test</scope>
        </dependency>
		
3 - supprrimer la dépandance rajouter précédemment
4 - rajouter les dependances :

  <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-api</artifactId>
            <version>${junit.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-engine</artifactId>
            <version>${junit.version}</version>
            <scope>test</scope>
        </dependency>
		
5 - les annotations ne sont plus reconnu
remplacer le before par BeforEach
importer la dependance import org.junit.jupiter.api.Test;

6 - relancer les tests (OK)
7 - supprimer assertj du pom.xml
8 - remplacer le assert par Assertions.assertAll
9 - relancer les tests -> ok
10 - modifier 2 asserts et relancer les tests et dans la console vérifier que y a bien les 2 tests qui sont ko.
11 rendre les tests parametrable
